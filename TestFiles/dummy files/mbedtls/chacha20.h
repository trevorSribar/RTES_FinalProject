#ifndef DUMMY_MBEDTLS_CHACHA20_H
#define DUMMY_MBEDTLS_CHACHA20_H

/*
 * Dummy mbedTLS chacha20 header for non-crypto test builds.
 * terminalTest only needs ENCRYPTION_* constants from encryption.h,
 * and does not execute encryption functions.
 */

#endif
